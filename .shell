#!/bin/sh

shell_update() {
    cd ~
    [ -d .no.git ] && mv .no.git .git
    [ -d .git ] || return
    git fetch --all
    git reset --hard origin/master
    mv .git .no.git
}

shell_reload() {
    if [ -d ~/.shell.d ] ; then
        for f in ~/.shell.d/?*.sh ; do
            [ -f "$f" ] && . "$f"
        done
        unset f
    fi
}

shell_reload
